import { css } from 'styled-components';

export const ButtonSmallText = css`
  font-size: 14px;
  line-height: 18px;
  font-weight: 700;
`.join('');

export const ButtonBigText = css`
  font-size: 16px;
  line-height: 20px;
  font-weight: 700;
`.join('');
