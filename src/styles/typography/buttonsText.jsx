import { css } from 'styled-components';

export const ButtonSmallText = css`
  font-size: 1.4rem;
  line-height: 1.8rem;
  font-weight: 700;
`.join('');

export const ButtonBigText = css`
  font-size: 1.6rem;
  line-height: 2rem;
  font-weight: 700;
`.join('');
